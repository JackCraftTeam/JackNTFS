using log4net;
using log4net.Core;
using log4net.Repository;
using System.Diagnostics;

namespace JackNTFS
{
    /* 日志记录器 */
    internal class JackLogger
    {
/*        private static readonly ILog GlobalJackLogger = LogManager.GetLogger(typeof(JackLogger));

        private JackLogger() {}

        public static ILog GetGlobalJackLogger()
        {
            return GlobalJackLogger;
        }

        protected 
*/
    }
}